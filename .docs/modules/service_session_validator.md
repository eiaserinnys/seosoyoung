# service/session_validator.py

> 경로: `seosoyoung/soul/service/session_validator.py`

## 개요

세션 검증 모듈 (하위호환 re-export)

원본이 slackbot.claude.session_validator로 이동되었습니다.
기존 import 경로를 유지하기 위한 re-export 모듈입니다.

## 내부 의존성

- `seosoyoung.slackbot.claude.session_validator.SESSION_NOT_FOUND_CODE`
- `seosoyoung.slackbot.claude.session_validator.find_session_file`
- `seosoyoung.slackbot.claude.session_validator.validate_session`
